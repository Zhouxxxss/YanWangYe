server:
  port: 8106

spring:
  application:
    name: ywy-community
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:yanyan}

management:
  endpoints:
    web:
      exposure:
        include: health,info